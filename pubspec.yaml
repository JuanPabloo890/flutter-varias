name: actividadfinalflutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.20
  go_router: ^14.2.1
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  webview_flutter: ^4.0.10

flutter:
  uses-material-design: true
